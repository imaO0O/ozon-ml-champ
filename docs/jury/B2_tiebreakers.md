# B2. Таблица tie-breaker'ов

Жюри при отборе финалистов смотрит, помимо RMSLE, два дополнительных
показателя: нормированный Gini по предсказаниям и относительную ошибку
суммарного предсказанного GMV. Здесь сведены оба по всем кандидатам на
финальные решения.

Числа взяты из журналов `models/experiments.csv` и `submissions/log.csv`,
источник каждого указан. Ничего не пересчитывалось на глаз.

## Откуда что берётся

**Gini по сабмиту вычислить нельзя** — `metrics.gini_norm` требует истинных
значений, а для тестового окна их нет и не будет до раскрытия private.
`calibrate.py` его тоже не печатает. Поэтому Gini здесь — **валидационный**,
со среза 2026-01-15: колонка `gini_blend` в `experiments.csv`, она же
`val_gini` в `log.csv`.

**Ошибка суммы** в колонке «сырая» — это `sum_bias_blend` на той же валидации,
то есть насколько предсказанная сумма GMV расходится с фактической. Знак
всюду отрицательный: модель занижает сумму примерно вдвое.

**Оценка суммарного GMV теста** получена умножением суммы предсказаний файла
на скалярный множитель ×2.002 из `calibrate.py`. Оговорки к этому числу —
в конце документа, они существенные.

## Таблица: фактические кандидаты в финал

Редакция **28.08.2026**. Отсортировано по public RMSLE, все отправлены
и отвечены.

| кандидат | public RMSLE | Gini (вал.) | сумма предсказаний | оценка GMV теста |
|---|---|---|---|---|
| **`pair_q.csv`** ← первый слот | **1.6466941** | нет данных¹ | 10 158 303 | 20.34 млн |
| `pair_nost.csv` | 1.6467440 | нет данных¹ | 10 309 892 | 20.64 млн |
| `pair_w2.csv` | 1.6469740 | нет данных¹ | 10 259 812 | 20.54 млн |
| **`pair_ac_cal.csv`** ← второй слот | **1.6472270** | нет данных¹ | 9 867 737 | 19.76 млн |
| `cand_w2_cal.csv` (половина трека C) | 1.6472648 | нет данных¹ | 10 643 421 | 21.31 млн |
| `pair_ac.csv` | 1.6473892 | нет данных¹ | 9 822 874 | 19.67 млн |
| `mix_multi.csv` (наша половина) | 1.6476256 | нет данных¹ | 10 649 731 | 21.32 млн |
| `mix_year.csv` | 1.6479607 | нет данных¹ | 9 874 009 | 19.77 млн |
| `mix_stk.csv` (прежний рекорд) | 1.6490610 | нет данных¹ | 10 005 611 | 20.03 млн |
| **`stk_b64_q.csv`** | 1.6499646 | **0.7393** | 9 975 050 | 19.97 млн |
| **`xgb_ins_q.csv`** | 1.6517475 | 0.7377 | 9 951 866 | 19.94 млн |

¹ Gini для составов не считается: он требует истинных значений, а составы
собраны прямо на тесте из уже откалиброванных файлов и валидационного
двойника не имеют. Валидационный Gini есть только у одиночных моделей.

**Оценка суммы у финалистов расходится на 0.9 млн** (20.64 против 19.76),
и это не ошибка, а следствие калибровки: обе руки приведены к одному уровню
`log1p` (2.32912) и одной дисперсии (2.6408), но **сумма в исходной шкале
от этого не фиксируется** — экспонента переносит разницу в форме в разницу
сумм. Если жюри смотрит на сумму, надо знать: у нас она **побочный продукт**
подгонки под RMSLE, а не оптимизируемая величина.

**Что изменилось для tie-breaker'а с прошлой редакции.** Появился
`stk_b64_q` — стекинг на сети с распределительной головой. Он делит первое
место по Gini со `stack3_quad` (0.7393), но по RMSLE лучше его на 0.0010,
то есть **доминирует прежнего чемпиона по ранжированию по обоим критериям
сразу**. Если жюри решает tie-breaker'ом, это лучший наш файл.

**Появился третий судейский критерий, которого у нас раньше не было
вовсе, — вероятностная состоятельность.** Рабочая сеть предсказывает
распределение, а не точку, и его качество замерено на обоих срезах
(`docs/jury/A3_probabilistic.md`): средний разрыв обещанной и наблюдаемой
вероятности нуля 0.0126 и 0.0120, после изотонической калибровки 0.0008
и 0.0003; CRPS 0.81286 и 0.85672; интервалы консервативны и не недокрывают.
Ни один из этих критериев не улучшает RMSLE — более того, калибровка
вероятностей его слегка ухудшает, и это записано прямо.

**Отвергнутая ветка, которую стоит упомянуть ради полноты:** `stkrk_q`
(1.6507540) — тот же стекинг с заменой признаков-датчиков времени на ранги.
На валидации он выигрывал на обоих срезах (+0.00036 и +0.00077), на
лидерборде проиграл 0.00079. Единственный за всю работу случай, когда
приёмка по знаку на двух срезах не подтвердилась лидербордом.

Оценки суммарного GMV у всех кандидатов сошлись в узкую полосу
19.6–20.6 млн — независимо от того, сколько в файле сети и сколько бустинга.
Для жюри это отдельный аргумент: величина, которую никто из нас не
оптимизировал, устойчива поперёк всех вариантов решения.

¹ **Почему пусто и что с этим делать.** Gini считается только по валидации:
`metrics.gini_norm` требует истинных значений, а для тестового окна их нет
до раскрытия private. Наши свежие кандидаты — бленды **отправленных файлов**,
и чтобы посчитать их валидационный Gini, нужны валидационные предсказания
каждого слагаемого. У нас есть только два из четырёх (наш ансамбль и сетевая
половина `cand4`), остальные — у трека C.

Закрывается одной командой на его стороне: `train.py --save-val-pred` кладёт
`(user_id, pred_log, target)` с выровненным уровнем, дальше `src/compose.py`
считает Gini состава при любых весах. Формат и инструмент уже в `main`.

**Что известно о Gini и без этих замеров.** Трек C посчитал его для своего
состава из четырёх участников: **0.7379** при RMSLE-оптимальных весах и
**0.7383**, если веса подобрать под Gini (ценой десятитысячной RMSLE). Отдельные
участники: бустинг 0.7377, сеть на дневных рангах 0.7346, стекинг на двух сетях
0.7391, сеть ранги-180 0.7337.

Порядок величин виден: **все наши кандидаты лежат в полосе 0.734–0.739**, и
выбирать между ними по Gini некорректно — две случайные половины по 125 000
клиентов расходятся сильнее.

## Прежняя таблица — кандидаты, снятые с рассмотрения

Оставлена для сопоставимости: на этих файлах измерялась ошибка суммы, которой
у новых кандидатов нет.

| кандидат | public RMSLE | Gini (вал., январь) | ошибка суммы, сырая | сумма предсказаний | оценка GMV |
|---|---|---|---|---|---|
| `lgbm_ens_full_calib.csv` | 1.6529357726 | 0.7378 | −51.1% | 10 063 305 | 20.15 млн |
| `mixed_ens_calib.csv` | не отправлялся | 0.7383 | −50.2% | 10 131 971 | 20.28 млн |
| `lgbm_ens_shift_scale.csv` | 1.6533489803 | 0.7378 | −51.1% | 10 691 103 | 21.40 млн |

## Что из этой таблицы следует

**Два tie-breaker'а упорядочивают кандидатов ровно наоборот.** Там, где Gini
посчитан, видно систематически:

| кандидат | место по RMSLE | место по Gini |
|---|---|---|
| `cand4_calib` | **выше всех с Gini** | **последнее** (0.7365) |
| `stack_calib` | ниже | 2 (0.7381) |
| `stack3_quad` | ещё ниже | **1** (0.7393) |
| `ranks_quad` | последнее | 3 (0.7376) |

**Наш лучший по метрике файл ранжирует клиентов хуже всех остальных.** Это надо
не прятать, а объяснить механизмом, и механизм измерен прямо здесь: сетевая
половина `cand4` даёт Gini 0.7342 против 0.7378 у бустинговой. Сеть попадает
по величине, но упорядочивает хуже, и в составе тянет Gini вниз.

**У стекинга эффект обратный, и это важно.** Там сеть входит признаком, а не
слагаемым: бустинг сам решает, где ей верить, и порядок не размывается —
`stack3_calib` даёт лучший Gini из всех (0.7393), обойдя даже чистый бустинг.
То есть проигрыш в ранжировании — свойство **бленда**, а не сети как таковой.

**Оговорка о значимости.** Весь разброс Gini между кандидатами — 0.0028
(от 0.7365 до 0.7393) при том, что две случайные половины по 125 000 клиентов
расходятся на 0.007 RMSLE (PLAN, раздел 6). Это оценка масштаба выборочного
шума по другой метрике, но порядок величины тот же. Утверждать, что один
кандидат «ранжирует лучше», можно только с этой оговоркой.

**Главный риск — ошибка суммы.** Сабмит по построению занижает суммарный GMV
примерно вдвое: RMSLE оптимизируется условным средним **логарифмов**, и
`expm1` от него систематически ниже условного среднего самой величины. Если
жюри считает относительную ошибку суммы прямо по нашему csv, мы получаем
около −50% при любом кандидате. Вносить поправку в сабмит нельзя — RMSLE сразу
ухудшится. Значит откалиброванную оценку суммы нужно подавать **отдельно**,
вместе с объяснением, почему она не совпадает с суммой по файлу.

Подтверждение переносимости поправки: множители подобраны на январском срезе,
проверены на декабрьском, ошибка суммы падает с 56% до 12.7% вне подгонки
(PLAN, раздел 5). Скалярная поправка переносится лучше подецильной.

**Калибровка не меняет Gini.** Все три поправки монотонны по предсказанию,
поэтому порядок клиентов сохраняется, а Gini зависит только от порядка. Для
сдвига и растяжения это очевидно. Для кривизны проверяется прямо: поправка
`p + γ·g`, где `g = u² − c·u` после ортогонализации, имеет производную
`1 + γ·(2u − c)`; при γ = −0.01325 и наблюдаемом размахе `u` она остаётся
заведомо положительной, то есть преобразование монотонно. Это согласуется
с измеренной ранговой корреляцией 1.000000.

Практический вывод: **калибровку можно применять, не опасаясь за tie-breaker
по ранжированию.** Она улучшает RMSLE и оценку суммы, а Gini оставляет тем же.

## Пробелы, которые надо закрыть до подачи

1. **Gini у пяти верхних кандидатов не посчитан.** Это бленды отправленных
   файлов, и для их валидационного Gini нужны валидационные предсказания каждого
   слагаемого — четыре штуки, из которых у нас два. Закрывается на стороне
   трека C одной командой `train.py --save-val-pred`, инструмент разбора
   (`src/compose.py`) уже в `main`.

   **Насколько это критично.** Весь наблюдаемый разброс Gini между кандидатами —
   0.0028, при том что две случайные половины по 125 000 клиентов расходятся
   на 0.007 RMSLE. То есть по этому показателю кандидаты неразличимы, и решение
   о финальной паре он всё равно не определяет. Пробел стоит закрыть ради
   полноты материалов, а не ради выбора.

2. **Ошибка суммы у новых кандидатов не измерена** — по той же причине.
   Для оценки GMV теста это не мешает: множитель ×2.002 применяется к сумме
   файла напрямую.

3. **`mixed_ens_calib.csv` без `lb_score`** — не отправлялся, из рассмотрения
   выбыл давно.

## Оговорки к оценке суммарного GMV — озвучивать вместе с числом

Число 21.4 млн (или 20.15 млн для актуального финального файла) — это
**оценка сверху**, и на то две независимые причины:

**Двойной учёт поправки.** Множитель ×2.002 измерен на некорректированных
предсказаниях, а применяется к уже сдвинутым и растянутым — часть поправки
учитывается дважды.

**Множитель измерен на другой модели.** `calibrate.py` обучает одиночную
конфигурацию `BASE_PARAMS`, а множитель затем умножает предсказания
ансамблевого откалиброванного сабмита. Это не та модель, к которой поправка
прикладывается.

В пользу оценки говорит перекрёстная проверка: фактический суммарный GMV
январского окна был около 21 млн, а зонд лидерборда показал, что тестовое
окно активнее ожиданий модели. Прежняя оценка 17.45 млн по
некорректированному сабмиту выглядит заниженной.

## Как пересобрать таблицу

Gini и ошибка суммы — из журналов, прогонов не нужно: колонки `gini_blend`
и `sum_bias_blend` в `models/experiments.csv`, они же `val_gini` и
`val_sum_err` в `submissions/log.csv`.

Оценка суммарного GMV — отдельным прогоном на файл (обучает две модели
заново, это минуты, а не секунды):

```bash
python -u src/calibrate.py --apply submissions/<файл>.csv
```

Путь задавать относительно корня репозитория: абсолютный путь на дисках
кроме `C:` в текущей версии `calibrate.py` обрабатывается неверно.
