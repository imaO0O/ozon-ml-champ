.venv/
__pycache__/
*.py[cod]

# Данные соревнования и кэш признаков — не в репозиторий
data/

# Веса и сабмиты не коммитим, а вот журналы — обязательно:
# по ним команда сравнивает эксперименты, а жюри проверяет воспроизводимость.
models/*
!models/experiments.csv
# Журнал перебора гиперпараметров: конфигурации и метрики, предсказаний нет.
# Отслеживается с 24.08; исключение записано 30.08, когда выяснилось, что
# правило и факт разошлись — файл жил в git только по истории.
!models/lgb_grid.csv
submissions/*
!submissions/log.csv

# CatBoost пишет логи обучения в рабочую папку
catboost_info/

.ipynb_checkpoints/
.idea/
.vscode/

# файлы передачи между треками — уходят напрямую, не через git
handoff_*.npz
