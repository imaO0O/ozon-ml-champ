# План работ

Базовая линия зафиксирована: **RMSLE 1.68538** на валидации (cutoff 2026-01-15),
Gini 0.738. Наивная авторегрессия из baseline организаторов — 2.195, константный
ноль — 3.204. Всё дальнейшее сравнивается с 1.68538.

## Привязка к лидерборду и потолок измеримости

| сабмит | валидация | public |
|---|---|---|
| `lgbm_0810_2348.csv` (одиночная модель) | 1.68538 | **1.65606** |
| `lgbm_ens_0811_1436.csv` (ансамбль) | **1.68402** | 1.65649 |

Два сабмита, отличающиеся на 0.0014 по валидации, разошлись на 0.0004 по public
**в противоположную сторону**. Предсказания при этом коррелируют на 0.9997 —
модели почти одинаковы, и обе оценки меряют разницу, сопоставимую со своей
погрешностью.

Это подтверждает на лидерборде то, что мы до этого увидели на двух валидационных
срезах: **эффекты меньше примерно 0.002 здесь неизмеримы никаким доступным нам
способом**. Ни валидация (расхождение между срезами ~0.001), ни public (50 000
клиентов из 250 000) не различают такие модели. Все сегодняшние микроулучшения
лежат под этим порогом.

Практические следствия:

* не тратить сабмиты на проверку эффектов меньше 0.002 — лидерборд на них
  не отвечает, а попыток пять в день на команду;
* браться только за то, где ожидаемый эффект крупнее порога: другие бустинги
  и их бленд, нейросетевая ветка, устойчивость к сдвигу уровня (0.06 в стресс-тесте);
* два финальных решения выбирать **максимально непохожими** друг на друга
  (например, LightGBM-ветка и CatBoost-ветка), а не двумя вариантами одного и
  того же: при разнице в 0.0004 второй вариант не добавляет ничего, кроме копии.

## Единый протокол — иначе числа команды несравнимы

Любой эксперимент меряется одинаково, иначе прирост невозможно отличить от
разницы в постановке:

* обучение на 5 срезах (2025-08-18 … 2025-12-16), валидация на 2026-01-15;
* метрика — `rmsle_blend` из `models/experiments.csv`, туда `train.py` пишет
  строку сам (коммит, версия признаков, число признаков, все три RMSLE);
* нельзя сравнивать свой прогон с чужим числом из чата — только строки журнала
  с одинаковым `feat_ver` или с явной пометкой, что менялось.

Первая привязка к лидерборду: валидация 1.68538 → public **1.65606**, то есть
public на 0.029 мягче. Это одно наблюдение, а не закон: пока оно годится как
поправка на глаз, но решения принимаются по валидации. Смысл столбца `lb_score`
именно в том, чтобы после нескольких сабмитов стало видно, переносятся ли
приросты с валидации на лидерборд один к одному.

Кэш признаков привязан к хешу `features.py` + `config.py`: после изменения кода
признаков он пересоберётся сам (~20 секунд на срез). Руками ничего чистить не надо.

## Направления, по убыванию ожидаемой отдачи

### 1. Сдвиг уровня площадки между обучением и тестом

Проверено по данным: **праздничных пиков в этом логе нет**. Относительно
локальной медианы 23 февраля даёт 0.954, 8 марта — 0.923, чёрная пятница —
0.891, 31 декабря — 0.710; максимум за весь год — 1.19 и приходится на
ничем не примечательные даты. Календарные эффекты не пережили обезличивание,
поэтому «праздничных» признаков делать не нужно.

Реальный риск другой — уровень площадки плывёт. GMV на пользователя за 30 дней
(знаменатель — все 250 000):

| окно с | GMV/чел | доля покупателей |
|---|---|---|
| 15.01.2025 | 57.6 | 36.7% |
| 12.02.2025 | 66.7 | 40.2% |
| 20.08.2025 | 88.9 | 49.6% |
| 03.12.2025 | 110.6 | 57.6% |
| 14.01.2026 | 84.1 | 54.1% |

За год уровень почти удвоился, в декабре пик, в январе провал. Год назад окно,
аналогичное тестовому, было на 16% выше предыдущих 30 дней (+0.17 в шкале
log1p). Модель, выученная на абсолютных рублях августа–декабря, переносит на
февраль-март уровень обучающих срезов — и промахивается сразу по всем клиентам.

Что с этим делать (по возрастанию риска):

* признаки-доли вместо абсолютных сумм — блок `platform`, уже в репозитории:
  доля пользователя в суточном GMV площадки от уровня не зависит. На штатной
  валидации он нейтрален (1.68538 против 1.68520 — шум), но под искусственным
  сдвигом уровня +22% (обучение на летних срезах, валидация на декабрьском)
  выигрывает 0.0019 RMSLE и 0.0015 Gini, то есть делает ровно то, для чего
  сделан. Проверено одной конфигурацией стресса, не серией;
* больше срезов с разными уровнями (см. пункт 2), чтобы модель увидела режимы;
* глобальная поправка уровня в log-шкале — только с явной оценкой риска:
  прошлогодний январь-февраль рос на растущей площадке, а с декабря 2025 рост
  остановился (110.6 → 84.1). Поправка «как в прошлом году» здесь не обоснована.

Буквальный признак «GMV в это же окно год назад» **неприменим**: для тестового
среза год назад есть (февраль-март 2025), а для обучающих это 2024-й, которого
в данных нет. Признак был бы пустым во всём обучении и заполненным только
в тесте — модель не смогла бы приписать ему вес.

### 2. Больше обучающих срезов — проверено, не работает

Шаг 14 дней вместо 30 даёт вдвое больше примеров ценой пересечения окон таргета.
Измерено на штатной валидации:

| прогон | RMSLE | строк |
|---|---|---|
| шаг 30 | **1.68538** | 1 205 328 |
| шаг 14, сетка от 2026-01-15 | 1.69846 | 2 158 685 |
| шаг 14, сетка от 2025-12-16 | 1.69284 | 2 406 149 |

Два эффекта разведены отдельными прогонами. Сетка от 2026-01-15 теряет из-за
карантина самый свежий допустимый срез (2025-12-16), и это стоит 0.0056.
Оставшиеся 0.0075 — цена самого перекрытия: соседние окна делят 16 дней из 30,
примеры почти дублируются, а вес старых периодов удваивается, и модель
смещается к старым режимам.

Вывод: количество срезов здесь не помогает, **свежесть важнее плотности**.
Шаг 30 остаётся умолчанием, `--stride` — только для экспериментов. Не тратьте
на это второй заход.

Взвешивание срезов по свежести (`--halflife`, вес падает вдвое за H дней)
проверено следом и **тоже не даёт рычага**:

| полураспад | RMSLE | перекос суммы |
|---|---|---|
| без весов | 1.68538 | −49.8% |
| 45 дней | 1.68486 | −51.5% |
| 90 дней | 1.68529 | −51.1% |
| 180 дней | 1.68449 | −50.5% |

Зависимость немонотонна, весь разброс 0.0009 при чувствительности прогона
к безразличным возмущениям (порядок колонок) 0.0001–0.0002. Полураспад 180
дней на диапазоне срезов 30–150 дней — это почти равные веса, то есть предел
совпадает с базовой линией. Направление исчерпано: чтобы надёжно отличить 180
от 45, нужна серия с разными сидами, а это дороже возможного выигрыша.

Побочное наблюдение: все взвешенные версии сильнее занижают суммарный GMV
(до −51.5% против −49.8%). На RMSLE это не влияет, но tie-breaker жюри
по суммарному GMV портит.

### 3. Гиперпараметры — проверено, переносимого выигрыша нет

24 конфигурации случайным поиском (`src/tune.py`, журнал `models/tuning.csv`),
две серии. На срезе 2026-01-15 лучшая дала 1.68423 против 1.68612 у умолчания,
и внутри серии число листьев сортировало результаты монотонно и без исключений
(31 < 63 < 127 < 255 < 511, дальше 1023 уже хуже).

Проверка на срезе 2025-12-16, которого перебор не видел, всё это опровергла:

| конфигурация | 2026-01-15 | 2025-12-16 |
|---|---|---|
| умолчание (127 листьев) | 1.68612 | **1.74166** |
| победитель перебора | **1.68423** | 1.74181 |
| умолчание, только листья 511 | 1.68505 | 1.74247 |

Победитель, выигрывавший 0.0019 на «своём» срезе, на чужом проигрывает.
Изолированная замена одного числа листьев ведёт себя так же: −0.0011 на одном
срезе, +0.0008 на другом. Умолчания не меняем.

**Методологическое следствие, важнее самого результата.** Расхождение оценок
между двумя валидационными срезами — порядка 0.001. Значит, эффект меньше
примерно 0.002 одним срезом померить нельзя: он тонет в различии режимов.
Все наши «улучшения» такого размера (веса срезов — 0.0009, блок platform —
0.0002) находятся ниже этого порога и не доказаны.

Отсюда правило для команды: **любой эффект меньше 0.002 проверять минимум на
двух валидационных срезах** (2026-01-15 и 2025-12-16) и принимать, только если
он положителен на обоих. Одного среза достаточно лишь для крупных эффектов —
таких, как проигрыш шага 14 дней (0.0075).

### 3а. CatBoost — проверен, в рабочий состав не взят

CatBoost на CPU оказался и быстрым, и равным LightGBM: 1.68632 против 1.68612
на штатном срезе, 459 итераций за 2.9 минуты (0.38 с/итерация). Видеокарта
для него здесь не нужна.

Смешение семейств в ансамбле (5 LightGBM + 2 CatBoost против 5 LightGBM):

| | январь | декабрь | под сдвигом уровня |
|---|---|---|---|
| только LightGBM | 1.68423 | 1.74118 | 1.74948 |
| только CatBoost | 1.68593 | 1.74301 | 1.75024 |
| смешанный | **1.68402** | **1.74109** | **1.74887** |
| вклад смешения | +0.00021 | +0.00009 | +0.00061 |

Знак верный во всех трёх замерах, и под сдвигом уровня вклад втрое-вшестеро
больше, чем в штатном режиме — качественно ровно как у блока `platform`. Но
величина втрое ниже порога различимости, поэтому **рабочий состав остаётся
чисто LightGBM** (`MEMBER_SETS["lgb"]`, он же умолчание `train.py --ensemble`).

Состав `mixed` проверен и оставлен в коде: он пригодится как заведомо непохожая
вторая модель при выборе двух финальных решений — там разнообразие ценнее
десятитысячных на валидации.

Не проверено и осталось: XGBoost, нейросетевая ветка (обе — к сокомандникам
с видеокартами).

### 3б. Блок unit — принят, хотя выигрыш подпороговый

Юнит-экономика и регулярность (`src/features/unit.py`, 35 признаков): цена
единицы товара, регулярность в неделях и месяцах, разброс межпокупочных пауз,
будни против выходных, брошенные корзины. Это единственные величины, не
выводимые из сумм остальных блоков.

На одиночной модели с одинаковыми параметрами: 1.68612 → **1.68551** на январе
и 1.74166 → **1.74122** на декабре. Положительно на обоих срезах, но около
0.0005 — ниже порога. В полном пути с ансамблем эффект исчезает: 1.68402 без
блока против 1.68420 с ним. Из 35 признаков в топ-20 по вкладу входит один —
`unit_price_cv`.

Блок оставлен: он бесплатен и выиграл в контролируемом сравнении, где отбирать
было не из чего. Отсюда общее правило по подпороговым эффектам: **принимаем,
если они ничего не стоят, отклоняем, если за них надо платить сложностью**
(поэтому блок `unit` внутри, а CatBoost в ансамбле — нет).

### 3в. Модель остатка поверх уровня пользователя — проверено, не работает

`src/offset.py`. Идея: подать `log1p(GMV за прошлые 30 дней)` как init_score и
учить только отклонение, чтобы уровень переносился напрямую, а не выучивался
в наблюдённом диапазоне.

| постановка | обычная | остаток | разрыв |
|---|---|---|---|
| штатный январь | **1.68551** | 1.69371 | 0.0082 |
| штатный декабрь | **1.74122** | 1.74603 | 0.0048 |
| сдвиг уровня +22% | **1.75185** | 1.75597 | 0.0041 |

Проигрывает во всех трёх. Разрыв монотонно сокращается со сдвигом режима, то
есть механизм качественно работает, но преимуществом не становится. Для
бустинга смещение — лишь другая точка старта, и модель корректирует его сама;
вдобавок у 46% пользователей GMV за прошлые 30 дней нулевой, и смещение для
половины выборки тождественно ноль. Направление закрыто.

### 4. Нейросетевая ветка

Организаторы прямо намекают на токенизацию дневного поведения. Практичный путь:
последовательность дневных событий → GRU/трансформер → предсказание таргета,
а скрытое состояние дополнительно отдать бустингу как признаки. Даже средняя
сеть в бленде обычно даёт 0.01–0.02 RMSLE.

### 5. Калибровка суммарного GMV — сделано (`src/calibrate.py`)

RMSLE-оптимальное предсказание систематически занижает суммарный GMV — это
математика метрики, а не ошибка: оптимум достигается на условном среднем
логарифмов, а `expm1` от него ниже условного среднего самой величины.
В сабмит поправка не идёт, RMSLE сразу ухудшится.

Множители подобраны на январском срезе и проверены на декабрьском:

| | январь (подгонка) | декабрь (проверка) |
|---|---|---|
| без поправки | 50.04% | 56.38% |
| скалярная (x2.002) | 0.00% | **12.70%** |
| подецильная | 0.00% | 13.72% |

Ошибка суммы падает с 56% до 12.7% вне подгонки. Простая поправка переносится
лучше сложной: подецильная идеально села на свой срез и проиграла на чужом.
Рабочей считаем скалярную.

**Оценка суммарного GMV на тестовом окне: 17.45 млн** (сырая сумма сабмита —
8.72 млн). Числа в `models/calibration.txt`.

### 6. Смещение уровня измерено напрямую — самый крупный найденный эффект

`src/errors.py` (структура ошибки) и `src/bias.py` (смещение и оптимальный сдвиг).
Модель переносит на целевое окно средний уровень своих обучающих срезов, поэтому
её среднее смещение в log1p-шкале прямо следует за уровнем целевого окна:

| валидация | целевое окно | смещение | лучший сдвиг | выигрыш RMSLE |
|---|---|---|---|---|
| 2025-11-16 | ноя–дек, уровень 110 (пик) | +0.027 | +0.03 | +0.0002 |
| 2025-12-16 | дек–янв, уровень 100 | −0.099 | −0.10 | +0.0028 |
| 2026-01-15 | янв–фев, уровень 84 (провал) | **−0.222** | −0.22 | **+0.0147** |

Обрезка снизу нулём ни при чём: отрицательных предсказаний 0.00%.

Это **на порядок больше всех остальных найденных эффектов вместе взятых**
(признаки, ансамбли, гиперпараметры дали суммарно около 0.002). И исправляется
одним числом — глобальным сдвигом всех предсказаний в log1p-шкале.

Загвоздка: сдвиг равен разнице уровней между обучающими срезами и целевым окном,
а уровень тестового окна неизвестен и данными не выводится. Зато он **точно
измеряется лидербордом**: RMSLE отдаётся с полной точностью, поэтому

    b = (MSE(0) - MSE(d) + d^2) / (2*d)

даёт смещение из одного сабмита при сколь угодно малом d. Отсюда рабочая
процедура: зонд с d = -0.05 (риск не больше 0.001 RMSLE, если смещения нет),
затем сабмит с оптимальным сдвигом b. Инструмент — `src/probe_shift.py`.

Предупреждение: b оценивается по 50 000 клиентов public, стандартная ошибка
около 0.007. Для одного глобального параметра это точно, подгонки под public
почти нет — но переносить на private стоит именно одно число, а не
посегментные поправки.

### 6а. Уровень тестового окна измерен зондом — противоречие разрешено

Офлайн вопрос был неразрешим: модель ожидала спад на 17% относительно января,
прошлогодняя сезонность обещала рост на 16%. Зонд лидерборда ответил точно.

| сабмит | сдвиг | public RMSLE |
|---|---|---|
| `lgbm_ens_0811_1436.csv` | 0 | 1.6564852698 |
| `probe_d-0.05_0811_1740.csv` | −0.05 | 1.6589835799 |

Отсюда `b = (MSE0 − MSE1 + d²)/(2d) = **+0.0578**`: модель **занижает**,
тестовое окно активнее, чем она ожидает. Прошлогодняя сезонность оказалась
права по знаку, но втрое слабее по величине (+0.058 против +0.17).

Оптимальный сдвиг равен b, ожидаемый RMSLE **1.65548** (выигрыш 0.00101).
Сабмит: `lgbm_ens_shift_0578.csv`.

Цена информации — 0.0025 RMSLE на зонде. Оправдана: без него направление
выбиралось бы рассуждением, а оно дважды подряд оказывалось неверным
(сначала «вверх на 0.15» по прошлогодней сезонности, потом «вниз» по смещению
на валидации — тестовое окно не похоже ни на то, ни на другое).

**Урок для команды.** Валидационное смещение (−0.222 на январском срезе) **не
переносится** на тестовое окно: финальная модель обучается в том числе на
январском срезе, поэтому её уровень уже другой. Измерять смещение нужно там,
где оно применяется, а единственный доступный измеритель для тестового окна —
лидерборд.

### 6б. Калибровка предсказаний зондами — самый результативный приём

Лидерборд отдаёт RMSLE со всеми знаками, поэтому любая одномерная поправка
вычисляется из **одного** сабмита аналитически. Проверено: сдвиг +0.0578
предсказывал 1.65548, лидерборд дал 1.6554754790 — совпадение до пятого знака.
Формула точная, а не приближённая.

Найдены две независимые поправки (растяжение идёт вокруг среднего, поэтому
не меняет уровень — параметры ортогональны и подбираются последовательно):

| преобразование | параметр | public RMSLE |
|---|---|---|
| исходный ансамбль | — | 1.6564852698 |
| зонд уровня | d = −0.05 | 1.6589835799 |
| **сдвиг уровня** | **d = +0.0578** | **1.6554754790** |
| зонд размаха | a = 1.04 | 1.6534999295 |
| **сдвиг + растяжение** | **a = 1.0545** | ожидается 1.6533492 |

Итог: **1.65649 → 1.65335, то есть 0.0031** — втрое больше всего, что дали
за день признаки, ансамбли и гиперпараметры вместе взятые.

Смысл находок: модель систематически **занижает** (тестовое окно активнее её
ожиданий) и **сжимает** предсказания (правда разбросана сильнее). Второе —
следствие обучения в другом режиме: под неопределённостью модель стягивает
предсказания к середине.

Риск подгонки под public мал: оба параметра глобальные и оценены по 50 000
клиентов. Стандартная ошибка растяжения около 0.005 при измеренном отклонении
0.0545 — то есть эффект на порядок больше своей погрешности. **Посегментные
поправки уже опасны** (у сегмента в 5 000 клиентов погрешность вчетверо выше),
и без явной оценки этого риска их брать не стоит.

## Разделение, чтобы не конфликтовать в одних файлах

| Трек | Файлы | Железо |
|---|---|---|
| Признаки (1) | `src/features/<свой блок>.py` — по модулю на человека | CPU |
| Валидация и калибровка (2, 5) | `config.py`, `train.py`, `metrics.py` | CPU |
| Бустинги и тюнинг (3) | `models.py`, конфиги параметров | GPU (5060/5070) |
| Нейросеть (4) | отдельный модуль, не трогает существующие | GPU (5060/5070) |
| Сборка ансамбля и сабмиты | `predict.py`, журналы | любое |

Признаки уже разложены на независимые блоки (`src/features/`), поэтому в один
файл никто не пишет: свой блок — свой модуль плюс одна строка импорта. Вклад
блока меряется флагом `--blocks` (см. README). Это снимает основной источник
конфликтов при параллельной работе.

## Правила работы

* Один эксперимент — одна ветка и один PR. В описании PR: строка из
  `experiments.csv` до и после.
* В `main` попадает только то, что не ухудшает базовую линию.
* Сабмитов 5 в день **на всю команду**. Договорённость: 3 слота под проверенные
  улучшения (прирост на валидации от 0.003), 2 — под разведку.
* Каждый сабмит пишется в `submissions/log.csv` автоматически; после ответа
  лидерборда в ту же строку руками вписывается `lb_score`.
* Данные и веса в репозиторий не кладём — только код и журналы.

## Выбор двух финальных решений

Соотношение public/private — 20/80, то есть на public всего 50 000 клиентов и
переобучиться под него легко. Разумный набор: одно решение — лучшее по валидации,
второе — самое устойчивое (лучшее среднее по нескольким валидационным срезам,
а не по одному). Выбирать по валидации, а не по public-лидерборду.
