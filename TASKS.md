# Задачи

Состояние на 12.08.2026. **Лучшее решение команды — `blend_sh_final.csv`,
public RMSLE 1.6516874** (бленд сети с бустингом, ветка `track-c-seq-nn`).

**Срок: 31 августа 2026.** Остаётся 19 дней и около 95 неиспользованных
попыток — бюджет сабмитов не дефицит, поиск продолжаем.

**Что уже исчерпано.** Тринадцать направлений моделирования дали ноль или минус
(список с числами — PLAN.md, раздел 4). Калибровка обеих моделей завершена:
у бустинга три поправки дали 0.0035, у бленда сдвиг уже применён, а растяжение
измерено и не требуется (1.1 сигмы).

**Где ещё может быть эффект.** Всё, что перебирало модели и гиперпараметры,
дало ноль. Работали только три типа действий: принципиально другой класс модели
(сеть — 0.0012), калибровка через измерение (0.0035) и **новое представление
данных** (ранги — 0.0042 и 0.0013 на двух срезах). Значит искать надо там же,
а не в очередной конфигурации бустинга.

**Свежий результат: блок `ranks` принят.** Процентильные ранги 27 величин
внутри среза дали 1.68572 → 1.68151 на январе и 1.74150 → 1.74016 на декабре —
крупнейшее улучшение признаков за всю работу (прежний рекорд 0.0006).
Механизм и числа — PLAN.md, раздел 5.

**Треку C: добавить ранги в признаки сети.** Деревья используют ранги как
пороги, сеть может использовать их совсем иначе — это независимый шанс на
эффект в бленде. Блок уже в `main`, включается сам.

## Что даёт результат — итог по всей работе

| источник | вклад |
|---|---|
| калибровка зондами лидерборда (бустинг) | **0.0035** |
| нейросеть в бленде с бустингом | **0.0012** |
| ансамбль 5 конфигураций | 0.0014 на валидации, −0.0004 на public |
| всё остальное: гиперпараметры, шаг срезов, веса срезов, CatBoost, блок `unit`, глубина истории, модель остатка | 0 или хуже |

## Трек A — ноутбук, CPU: сабмиты, калибровка, учёт

**A0. Калибровка модели с рангами — отправить `lgbm_ranks_calib.csv`.**
Измерено: `lgbm_ranks_shifted.csv` дал 1.6551048346, зонд
`probe_ranks_a102.csv` — 1.6537225749, отсюда α = 1.0586 при значимости
12 сигм и ожидаемый RMSLE **1.6526617**.

Важный итог для планирования: сами ранги дали на public всего +0.00037
(1.6554755 → 1.6551048 при одинаковой калибровке), тогда как на валидации
давали +0.0033. **Калибровка той же модели дала 0.0024 — вшестеро больше, чем
улучшение признаков.** Ожидайте, что выигрыш на валидации придёт на лидерборд
в разы меньшим, а калибровка переносится один в один.

**A1. Пока ответа нет — развивать ранги, это сработавшее направление.**
Блок дал 0.0042 и 0.0013 на двух срезах, крупнейшее улучшение признаков за всю
работу. Очевидные продолжения, каждое проверяется без сабмитов:

* **разности рангов** — `rk_gmv_30 − rk_gmv_365` и подобные: показывают, растёт
  ли пользователь внутри когорты или падает, чего абсолютные тренды не ловят;
* **ранги внутри сегментов**, а не по всему срезу: у активных и спящих клиентов
  распределения разные, и общий ранг их смешивает;
* **расширить список ранжируемых величин** — сейчас 27 из 242 признаков.

Проверять по правилу двух срезов, обе руки одной командой.

**A2. Дежурная перекалибровка.** Если трек C меняет вес сети или архитектуру —
модель другая, и поправки надо получить заново. Порядок:

```bash
python -u src/probe_shift.py --derive-shift --source <новый>.csv      # сдвиг, бесплатно
python -u src/probe_shift.py --source <новый>.csv --alpha 1.02 --out probe_a.csv
python -u src/probe_shift.py --solve --alpha 1.02 --var-p <из вывода> --mse0 <RMSLE базы> --mse1 <RMSLE зонда>
```

Шаг зонда — **малый** (1.02, не 1.04): направление не угадывается, а цена
ошибки растёт как квадрат шага. Ожидание: при малом весе сети растяжение снова
понадобится, при большом — нет (сеть сама расширяет размах).

**A3. Учёт сабмитов и финальный выбор.** Журнал `submissions/log.csv` ведётся
автоматически, `lb_score` вписывается руками. К концу этапа в нём должно быть
видно, какие два файла выбраны финальными и почему.

**A4. Проверить, что финальные решения действительно отмечены** на платформе
до закрытия этапа. Процедурный риск, который стоит дороже любых 0.002.

## Трек B — ноутбук, CPU: материалы жюри

**B1. Воспроизводимость с чистой машины.** Клонировать репозиторий в пустую
папку, пройти README сверху вниз, убедиться, что решение собирается без правок
и без подсказок. Всё, что придётся додумывать, — дописать в README.

**B2. Таблица tie-breaker'ов.** Жюри смотрит Gini по предсказаниям и RMSPE
суммарного GMV. Свести по финальным кандидатам:

```bash
python -u src/calibrate.py --apply submissions/<файл>.csv
```

Известно: калибровка **не меняет Gini** (все три поправки сохраняют порядок
клиентов, ранговая корреляция 1.000000), а у бленда Gini на январе 0.7348
против 0.7378 у чистого бустинга — то есть по ранжированию бустинг чуть лучше.

**B3. Черновик питча.** Что показывать, кроме результата:

* зондирование лидерборда как измерительный прибор: три поправки, каждая
  предсказана до пятого знака и подтверждена ответом;
* вывод уровня тестового окна, после которого зонд на уровень не нужен никому;
* тринадцать честно задокументированных отрицательных результатов с числами —
  редкость на таких соревнованиях;
* найденный предел сравнимости прогонов (0.0009 между пересборками кэша) и
  правило двух валидационных срезов.

## Трек C — ПК с видеокартой: ранги в сеть и вторая кандидатура

**C0. Добавить блок `ranks` в обе половины бленда — делать первым.**
Блок уже в `main` и включается сам, пересборка признаков занимает две минуты.
Бустинговой половине он даст те же 0.003, что и нам. А **сеть может извлечь
из рангов принципиально другое**: деревьям ранг нужен как удобный порог,
а сети — как готовая нормировка входа, которой у неё сейчас нет вовсе.
Это независимый шанс на эффект поверх нашего, и он не стоит ни одного сабмита.

**C1. Вариант бленда с бо́льшим весом сети — вторая финальная кандидатура.**
Текущий вес 0.25 подобран под 50 000 клиентов public. Private вчетверо больше
и другая, поэтому вариант с весом около 0.35–0.40 — это страховка, а не потеря,
даже если на public он чуть хуже.

Обоснование измерено с двух сторон: все кандидаты внутри бустингового семейства
коррелируют на 0.9992–0.9997, то есть два лучших файла — это одно решение
дважды. **Настоящую непохожесть в команде даёт только сеть.**

**C2. Если остаётся время — улучшать сеть, а не бустинг.** Это единственное
место, где ещё возможен эффект крупнее порога: бустинг на этих признаках
выбирает практически всю доступную информацию, что подтвердили обе половины
команды независимо.

## Общие правила

* **Порог 0.002.** Эффект меньше — проверять на двух валидационных срезах
  (2026-01-15 и 2025-12-16) и принимать только при положительном знаке на обоих.
* **Обе руки сравнения — одной командой на одной версии кэша.** Между
  пересборками кэша числа расходятся на 0.0009 даже при одинаковых признаках.
* **Один эксперимент — одна ветка и один PR**, в описании строки из
  `models/experiments.csv` до и после.
* **Журналы обязательны.** `train.py` и `predict.py` пишут их сами; после ответа
  лидерборда впишите `lb_score` руками в `submissions/log.csv`.
* **Не трогайте чужие файлы.** Признаки разложены на блоки именно для этого:
  свой модуль плюс одна строка импорта в `src/features/__init__.py`.
* **Данные и веса в git не кладём** — только код и журналы.
